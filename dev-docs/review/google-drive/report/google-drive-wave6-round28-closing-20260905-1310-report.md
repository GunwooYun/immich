# 리뷰 요청 (사이클 종료 확인) — 관문 통과 이후의 델타가 정말 0인가

| | |
|---|---|
| 브랜치 | `feat/google-drive-album-sync-v3.1.0` |
| HEAD | `994d8835d` |
| 리뷰 대상 커밋 | `16562832b`, `994d8835d` + 이 리포트 |
| 직전 리뷰 | `../review/google-drive-wave6-round27-ledger-fallback-20260905-1245-review.md` (**배포를 막는 것 없음**, 사이클 종료 가능) |
| 증거 | `dev-test/google-drive/results/20260905-1304.txt` — 커밋 `16562832b`, dirty 표시 없음 |
| CI | `https://github.com/GunwooYun/immich/actions/runs/33943593503` (`994d8835d`) — **4잡 전부 success, 실패 애노테이션 0** |
| 작성 | 2026-09-05 13:10 |

## 이 라운드가 존재하는 이유, 그리고 물어보는 것 하나

round-27이 `ae64e0ae3`에서 **배포를 막는 것 없음**으로 관문을 통과시켰다. 그 뒤 두 커밋이 있고,
둘 다 round-27이 남긴 지적(N1·N2·N3)의 반영이다. §2.4가 "수정 자체도 리뷰 대상"이라 요청서를 쓴다.

```
$ git diff ae64e0ae3..994d8835d --stat -- server/src web/src ':!*.spec.ts'
(빈 출력)
```

**운영에 나가는 코드는 관문을 통과한 그대로다.** 바뀐 것은 `CLAUDE.md`(런북), medium 스펙,
그리고 리뷰·증거 파일뿐이다.

**물어보는 것은 이것 하나다: 위 진술이 참인가.** 참이면 이 사이클을 종료한다.
거짓이면 — 배포되는 산출물에 영향을 주는 변경이 저 안에 하나라도 있으면 — 그것을 지목해 달라.

## 반영한 것 (round-27 N1/N2/N3)

### 1. 체크리스트의 첫 관측이 존재하지 않는 필드를 가리켰다 (N2)

"설정 화면을 열기 전에 `getGoogleDriveStatus`의 `enabled`를 확인하라"고 적었는데
**그 필드가 없다.** 플래그는 `GET /api/server/features`의 `googleDrive`이고
(`server.service.ts:122`가 `isGoogleDriveEnabled`를 그대로 노출),
게다가 `getStatus` 호출 자체가 신원 프로브를 트리거하므로(`service.ts:632-639`)
**"열기 전"의 관측이 될 수 없었다.**

오류는 round-25 리뷰에서 나왔고 내가 응답 모양을 확인하지 않고 두 문서에 옮겼다. 배포 당일
1번 단계에서 막혔을 오류다. 확인 명령을 런북에 넣었다.

### 2. 런북의 자기모순 (N1)

"식별된 계정이 맞는 계정인가" 문단이 아직 `connectedAt`으로 논증하고 있었다 — 아홉 줄 아래
문단이 그 비교를 버렸다고 적은 상태에서. 정직한 버전은 **코드가 어느 쪽으로도 단정할 수 없다**는
것이고(배포 전 행은 `connectionId`가 없다), 그래서 손대지 않으며 판단이 사람 몫인 이유다.

### 3. medium 스펙의 격리가 우연이었다 (N3)

이 파일에는 `afterEach`가 없었다. 여러 라운드 동안 변이가 죽은 이유가 픽스처가 아니라 **앞선
describe가 남긴 행**이었고, 그중 둘(`:298`·`:562`)은 격리하면 실제로 살아 있었다. 그 둘을
명시적 픽스처로 고친 뒤이므로 이제 정리를 넣어도 안전하다 — 넣고 핵심 변이 4개
(`:48`·`:288`·`:298`·`:547`)가 그대로 죽는 것을 확인했다.

**공격 요청**: 이 `afterEach`가 **다른 테스트의 근거를 없애지 않았는지**. 53개가 통과하는 것과
53개가 여전히 같은 것을 증명하는 것은 다르다.

## 종료 조건

위 "운영 코드 델타 0" 진술이 참이고 `afterEach`가 기존 단언을 무력화하지 않았다면,
**이 사이클을 종료하고 배포로 간다.**
