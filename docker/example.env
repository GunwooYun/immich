# You can find documentation for all the supported env variables at https://docs.immich.app/install/environment-variables

# The location where your uploaded files are stored
UPLOAD_LOCATION=./library

# The location where your database files are stored. Network shares are not supported for the database
DB_DATA_LOCATION=./postgres

# To set a timezone, uncomment the next line and change Etc/UTC to a TZ identifier from this list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List
# TZ=Etc/UTC

# The Immich version to use. You can pin this to a specific version like "v2.1.0"
IMMICH_VERSION=v3

# Connection secret for postgres. You should change it to a random password
# Please use only the characters `A-Za-z0-9`, without special characters or spaces
DB_PASSWORD=postgres

# Google Drive album sync (this fork). The feature is on exactly when a client id and secret are
# present and a redirect URL can be worked out — there is no separate switch and no admin form.
# Leave these blank to keep it off.
#
# The redirect URL is normally derived from the External Domain server setting; set it explicitly
# only when that cannot be used, as on a LAN-only deployment reached through an SSH tunnel.
# The API key is optional and only powers the browser-side folder picker.
IMMICH_GOOGLE_DRIVE_CLIENT_ID=
IMMICH_GOOGLE_DRIVE_CLIENT_SECRET=
IMMICH_GOOGLE_DRIVE_REDIRECT_URL=
IMMICH_GOOGLE_DRIVE_API_KEY=

# The values below this line do not need to be changed
###################################################################################
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
